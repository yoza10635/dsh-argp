# PROPOSAL：共享承重 token 本体 —— 从"两套信任机制"到"一套 token 本体"

> **状态**：提案（未实施）。配套 `ARCHITECTURE.md` 阅读。v1.0，2026-09-14。
> **分期**：P1 = 组件 A（推断边）+ 组件 B（HLS 修复）；P2 = 组件 C（起源类）。
> **版本目标**：v1.2.0（minor：新边级别 + 新压缩档，均向后兼容，带配置开关）。

---

## 0. 为什么：两个结构性缺口

### 0.1 语义边通道是模型质量的单点故障

Stage-2 剪枝保护度（inDegree）**只由语义边计入**（`argp-graph-engine.ts:1427-1428`，`inDegree` 仅从 `edges` 计算）；确定性 A→R 边（`toolCallId` 配对，`1346-1357`）单独返回、不进 inDegree。

语义边目前只有两条来源通道，**两者都依赖模型输出**：

1. 回复内 `a.cites`：前缀匹配解析建边（`1368-1409`）；
2. CiteDeclarer `injectEdges`：每轮一次 LLM 调用声明窗口边（`peratom/cite-declarer.ts`，10 轮窗口、512 边缓存、失败静默无边）。

已知失效模式（README「模型要求」节 + 本仓库注释）：

- DeepSeek 系模型在系统提示与任务指令冲突时 **cites 声明为 0**（语义选择性归零）；
- 给 prompt 留出口后最佳实测声明率也只有 **43.6%**（10 轮）；
- CiteDeclarer 任何故障（超时/拒格式/解析失败）→ 该轮无边、静默降级。

后果：通道空窗时剪枝序退化为「拓扑序 + 最近引用轮」——**不变式仍成立**（append-only 日志 + recall 兜底，错误方向只往少剪错），但 README 实测的 **≈2.6× 召回效率收益丢失**：「最近被用到但未被声明」的数据原子会优先于未引用原子被整原子摘除。

现有设计对此只有**优雅降级**（宁可不保护），没有**选择性恢复**机制。这是方法论层面的单点故障：整个 lazy 侧的价值压在一条"模型愿意且能够声明"的通道上。

### 0.2 守卫的拒收经济学：拒收 = 全损

extract 硬拒路径（`peratom/compressor.ts:479-486` 工具档、`404-414` user-info 档）：`fidelityGuard` 缺任一高信号 token → 整条提案丢弃、原文保面、**压缩收益 = 0**。

哲学正确（错误方向只允许往"少压"错），但经济学极端：**90% 忠实的 extract 与 1% 忠实的 extract 待遇相同**（都整条拒收、收益全损）。同时：

- README 的第一条动机是「摘要改写中**精确 token 最先死**」——即：软内容（prose）可以损失，硬 token（路径/错误码/键值）不可；
- 现有两档在「硬/软 × 无损/有损」矩阵里只占两格：`extract` = 软无损+硬无损；`summary` = 软有损+**硬有损**（缺失入账 `summaryDropped` 供审）；
- **缺的正是「软有损 + 硬无损」这一格**。它是该矩阵中唯一同时满足"软内容可压"与"硬 token 零损失"的档位，也是 README 动机的问题本身的答案——但目前它的实现只能靠模型自觉，而模型恰是这一格最不可靠的执行者。

---

## 1. 方法论主张：一套词表，两种机制

高信号 token 词表（`gate.ts:292-300` `LOAD_BEARING_PATTERNS`：URL / 路径 / file:line / UUID / 十六进制哈希 / ALL_CAPS 错误码 / key=value）目前**只用于原子内**（guard 裁决副本保真）。

主张：**同一套词表同时作为原子间边的判定依据**。

- **保真由构造**（fidelity by construction）：硬 token 存活不再依赖模型能力，而是守卫的构造性保证（组件 B）；
- **选择性由派生**（selectivity by derivation）：模型沉默时，边仍可从逐字包含关系派生出来（组件 A）；
- **信任边界不变**：模型仍只提议、守卫仍裁定、Stage-2 仍 0 LLM 调用。模型的角色从「语义边的唯一来源」降格为「语义边来源中的强先验」——声明边仍是最强证据，推断边只是保底层。

一句话：**把"LLM 在环内、但戴着镣铐"从 Stage-1 推广到 Stage-2——环内的不再只有压缩提案，还有边的提议，而守卫的裁定从"否决"扩展为"补全"。**

---

## 2. 组件 A —— 推断边（derived-cites 保底层）

**定义**：对原子对 (A_i, T_j)（A_i 较新、T_j 较旧、同在窗口 W 内），若 T_j 的某个承重 token（长度 ≥ 6、且不在高频停词集）在 A_i 文本中**逐字出现**，则建推断边 A_i → T_j。

**构造**（全部落在 `buildGraph`，`argp-graph-engine.ts:1346`，0 LLM）：

1. **token 倒排**：建图时对窗口原子提取承重 token，建 token → 含该 token 的原子 id 列表（可复用现有 n-gram 索引层 `1359-1367` 做候选检索，或独立轻量倒排）；
2. **停词过滤**：出现在窗口 >15% 原子中的 token 不建边（防公共路径/端口/通用标识稀释信号；阈值进 config）；
3. **派生**：对每个 A 原子（近 W=20 轮，进 config），取其承重 token 命中的**更旧**数据原子（U/R，seq 严格更小）→ 候选边；
4. **去重**：同 (from,to) 已有 cites / inject 边则跳过——**声明先行**，与 `injectEdges` 现有去重（`1414-1423`）同纪律；
5. **上限**：每 A 原子 ≤ 8 条推断边（防 token 密集回复造成边爆炸）；
6. **级别**：`EdgeLevel` 新增 `'inferred'`，`EDGE_WEIGHTS.inferred = 1`（低于 contextual=2、supporting=5、critical=10，`123`）——推断边保护度高于无边原子、低于任何声明档。

**不变式（property test 可断言）**：

- **I-A1** 每条推断边满足逐字包含：∃ token t，t ∈ tokens(T_j.text) 且 t ⊆ A_i.text（构造性，无判断成分）；
- **I-A2** 0 LLM 调用：buildGraph 代码路径不触达任何 LLM adapter（代码审查 + 调用计数断言）；
- **I-A3** 声明边满配时剪枝序不变：推断边不新增任何已声明保护之外的保护 → 与无推断边跑次逐位一致（回归零扰动证明）；
- **I-A4** 0 声明条件下，被近 W 轮 A 的 token 引用的原子保护集 ⊇ 无推断边跑次（只增不减）。

**成本**：token 倒排 O(窗口总 token 数)/次建图；窗口限 20 轮、每原子 token 数有界，量级与现有 n-gram 建索引相当。

### 2.1 已知边界（spike40 实测，2026-09-15）

`npm run spike40` 以**受控植入真值**（唯一 handle 数据原子 + A 原子植入"真依赖 T / 仅提及 M"）量化 token 通道的判别力。结论：

| 项 | 实测 | 含义 |
|---|---|---|
| 无噪音基线（m=0） | precision = recall = 1.000 | token 通道本身无损，误连全部来自"共现≠相关" |
| 误连率曲线（\|T\|=2） | m=0 → 0%；m=1 → 33.3%；m=2 → 50%；m=3 → 60% | 精确等于 `m/(2+m)`：**每多一个"仅提及"，precision 掉 `1/(2+m)`** |
| 半公共 handle（DF=13.2% < 15% 停词阈） | 单例产生 4 条误连边（precision 0.200） | **F40-1 · HIGH**：停词是全或无二值，DF 略低于阈值的"通用 handle"全部漏网 |
| 每 A 上限拥挤（3 真依赖 + 6 提及 = 9 > 8） | recall 0.667，丢失最旧的真依赖 | **F40-2 · MEDIUM**：`maxEdgesPerAtom` 按 seq 降序截断，提及挤占名额时会丢真依赖 |

**能力边界声明（缺口 2：收益共生条件性）**：推断边是**纯保护、只增不减**（I-A4），错误方向只往"少剪"错——所以误连的代价不是错误剪枝，而是**压缩机会丢失**（代价侧分母，spike40 `S40-5` 已量化可计算）。由此该机制**只在"未声明通道空窗"时产生净收益**：一旦模型 cites 声明满配，推断边被去重（I-A3），收益归零而误连代价仍在（只是被声明边掩盖）。**"收益 > 误连代价"是一个条件性结论，不是无条件正收益。**

**观察项（§5 生命周期天花板）**：spike38 `S38-7` 实测 lifecycle 档依赖源存活 0/3 → A10 修正后 2/3——**2/3 是结构天花板**：第 3 个依赖源与引用方同轮退休、经 §5.4 链式解锁被带走，属闭包语义而非缺陷。让推断边豁免闭包会破坏 critical-only 守卫（不变量 2′），故不修，仅记录为能力边界。

### 2.2 真实语料校准（spike41，2026-09-15）

spike40 是**受控植入真值**（合成）。`npm run spike41` 换用本机真实 session
（`~/.dsh/sessions`，只读；见 `spike/lib/session-corpus.ts` 隐私铁律）做校准。
样本：4 个真实 session / 823 个 zstd 帧 / 2843 条带 seq 事件 / **646 原子 / 1,052,502 字符**；
读取器完整性 0 坏帧、0 非法行、**seq 缺口 0、重复 0**。

| 项 | 实测 | 含义 |
|---|---|---|
| 每 session 推断边 | 14 / 7 / 47 / 0（共 68） | 机制在真实数据上非空转 |
| 每 session maxDF | 17 / 5 / 14 / 10 | 真实 handle 的**最高**文档频率 |
| **15% 停词阈命中** | **0 个 token（4/4 session）** | **F41-1**：停词阈在真实语料上**从不触发** |
| 单 token 支撑边占比 | **61/68 = 89.7%** | **F41-2**：信号判别力弱（单 token 是真引用与巧合的共同形态） |
| 影响半径 | 30/462 = **6.5%** 数据原子被保护 | 代价侧影响面小（与误连率是两回事） |

**F41-1（MEDIUM）**：15% 停词阈防的是"全局公共标识"这一失效模式，而真实语料不产生它
（最大 DF 17，阈值 ≥ 33）→ 该守卫在生产中**结构性空转**。推论：spike38 `S38-5` 用 30% DF
样板 token 验收该守卫，**是按需构造的场景，不能代表生产**。

**F41-2（MEDIUM，措辞已校正）**：89.7% 的边只由**一个** token 支撑。**校正说明**：单 token 支撑
是**真引用与巧合提及的共同形态**——A 说"修 `src/db/pool.ts` 第 88 行"，对应的 R 就只共用那个路径
token，这是真引用。所以本项度量的是**信号判别力弱**（token 通道无法区分"真引用"与"巧合提及"），
**不是**"误连率更高"；真实误连率仍需人工/LLM 标注才能测。

**连带校正（撤回上一版建议）**：先前提议的"停词按 DF 衰减权重"（治 F40-1）与"≥k 独立 token
才建边"（治 F41-2）**都不是对症修法**：前者对低 DF 的单 token 边无从衰减；后者会**误杀真引用**
（真实场景最常见的真引用就是单文件/单定位形态）。**在拿到真实误连率标注之前，不应改门控。**

**机制定位（缺口 2 被真实数据加固）**：组件 A 是**低覆盖率（6.5%）、弱判别力（89.7% 单 token
支撑）、纯保护只增不减**的窄兜底——**只在模型声明通道空窗时才有价值**。它"钝"，但错误方向只往
"少剪"错，因此代价有界（不损正确性，只损压缩机会）。**真实语料没有降低它的有效性（受益侧
spike38 S38-3 的 3/3 保护不受影响），降低的是"稀有 handle 共现 ≈ 语义信号"这一信念。**

### 2.3 缺口的唯一解法：真实边标注（spike42）

上表只能测"判别力弱"，**测不出误连率**——那需要真值标签。`npm run spike42` 把真实语料上派生
的边做成可标注工作表（本机实测 **68 条：单 token 支撑 61 / ≥2 支撑 7**）并提供打分器。

**标注单位 = 配对**（A 摘录 + R 摘录 + 共同承重 token），**不是整轮对话**。理由：整轮会泄露
"后来发生了什么"这条捷径（该 session 里 `read` 被调 144 次、`recall` 仅 2 次——**静默降级是常态**，
agent 靠重读/重搜自己补，而不是 recall）。一旦用了这条捷径，量到的就变成"结果好不好"，
而非"token 共现能否代表语义引用"。

**协议**：
1. **盲化支撑强度**：工作表 `.blind.jsonl` 不含 support，support 单放 `.key.json`，标注完成前不打开
   —— 否则无法检验"支撑强度能否预测真值"。
2. **三分类**：yes（A 确实在引用 R 的内容）/ no（仅偶然共用 token）/ unsure（排除出分母）。
3. **只看出题信息**，不回看日志。
4. **打分**：`npm run spike42 -- --score <file>.blind.jsonl` → 总 precision + **分层 precision
   （单支撑 vs ≥2 支撑）+ Wilson 95% CI + 分层差 Δ**。

**判读规则（先定死，避免事后找理由）**：
- `Δ` 显著为正 → "≥k 独立 token 才建边"**有据**（代价：牺牲部分真引用换精度）；
- `Δ ≈ 0` → 支撑强度**无预测力**，**门控改动无据，不要动**（与 §2.2 撤回建议的结论一致）；
- 总 precision 本身即组件 A 的**真实误连率**（此前唯一缺失的分量）。

**隐私**：工作表含真实 session 摘录 → 只写 gitignored 的 `spike/out/labels/`，**绝不提交**；
仓库只允许出现聚合精度数字。

**另注（真实日志的免费观察 + 两处自我纠错）**：

- **声明通道近乎空窗（但非绝对为零）**：4 个 session 合计 **`cites` 声明 2 次**（a56061c2 一处 2 次
  / 136 条 assistant 原始回复 = **1.5%**；其余 3 个 session 为 0），而引擎做了 **265 次
  `compaction/prune`**（271 个 seq 被驱逐）→ 组件 A 在这些真实会话里基本是**唯一**的语义选择性通道。
  ⚠️ **纠错**：此前记为"cites 声明 0 次"是**错的**——计数必须只扫 **append 原始写入的
  `assistant/message` 正文**；扫全日志会把 replace 副本重复计入（本例虚增到 28）、把工具 schema
  的字段说明计入（本例 3 次）。
- **"驱逐后重读"（prune-then-reread）**：被驱逐内容涉及的路径中，**24/30（80%）** 在同 session
  内被再次 `read`/`grep`（另一 session 40/40）。⚠️ 这是**上限**——agent 本就会为自身理由重读文件，
  **可归因的驱逐代价必须由对照组做差**（同题关掉剪枝），单臂绝对值不可单独引用。
- **组件 B 无生产线观测**：`[restored]` 尾注真实落盘 **0 例**。⚠️ **纠错**：裸文本搜索 `[restored]`
  在本仓库得到 41 处**全是假阳性**——它们来自"对话在讨论 HLS 机制本身的源码"
  （这几个 session 读/改的正是 `token-ontology.ts`，于是 `return candidate + '\n[restored] ' + missing.join(' ')`
  这行源码本身出现在日志里）。正确判据 = **I-B3 构造性**：尾注 token 必须逐字 ⊆ 原文承重词表。
  **教训**：在"会话内容就是机制源码"的自指语料上，任何针对机制字面量的文本搜索都必须带构造性校验。

---

## 3. 组件 B —— HLS 修复档（hard-lossless / soft-lossy）

**触发**：模型选 `extract`、`fidelityGuard` 判定 missing 非空（现路径 `compressor.ts:484` 的 `continue` 处）。

**构造 v1（尾注补全）**：

```
repaired = candidate + "\n[restored] " + missing.join(" ")
```

- `missing` = 守卫缺失清单，按原文首次出现顺序、去重；
- 输出**按构造 100% 硬 token 保真**；保留最终 `fidelityGuard(repaired)` 调用作为**构造性断言**（平凡通过，构造回归即报警）；
- 原文永在 append-only 日志，`recall_detail` 契约不变。

**审计**：新增 `restoredByGuard` 清单（独立于 `fidelityMissing`）——「守卫补回了哪些 token」，与 `summaryDropped` 同级可审（台账化，不静默）。

**构造 v2（锚点内插，P1.5 可选）**：对每个 missing token，在 candidate 中检索原文邻近 8 字符所在的句子，命中则原位内插；未命中落尾注。目标：降低尾注的视觉与 token 开销（spike34 语料显示缺失通常 1~4 个 token，尾注开销有界）。

**不变式**：

- **I-B1** 对任意提案，repaired 通过守卫（property test：对 candidate 做随机 token 扰动后验证）；
- **I-B2** no-op 守卫（`≥95%` 视同 false，`470-473`）与 HLS 无冲突：no-op 判定先于 guard，repaired 只可能产生于 <95% 情形；
- **I-B3** 错误方向不变：修复**只添加**原文 token、不重写模型 prose、不删 candidate 任何字符——prose 部分有损性等同 summary 档的受控损失；
- **I-B4** 图侧兼容：HLS 原子的硬 token 逐字在场 → 前缀解析 / n-gram 索引 / 版本链键天然兼容，无需新分支；
- **I-B5（经济学门控，v1.2.0 实施）** 修复**代价盲**修正——仅当 `ROI = 净释放 / 尾注占用 ≥ θ`（缺省 θ=1）才修复，否则退回原文保面。见下。

**经济学门控（第一性判据：候选的 prose 收益 > 缺失 handle 的补全代价）**：

记 `L_orig` / `L_cand` / `L_rep` 为原文 / 候选 / 修复文本长度，则

```
增益 B   = L_orig − L_cand          （候选 prose 相对原文省下的字符）
代价 C   = L_rep − L_cand           （尾注占用 = len("\n[restored] " + missing.join(" "))）
净释放 N = L_orig − L_rep = B − C   （相对「原文保面」的预算净释放，可负）
ROI      = N / C                    （C = 0 → +∞）
```

门控取 `ROI ≥ θ`（`token-ontology.ts` `hlsRepairEconomics`，缺省 θ = `DEFAULT_HLS_ROI_THRESHOLD` = 1）。
θ=1 的语义是「尾注替自己买单」：净释放的预算至少与尾注占用相当。

- **N < 0（修复后比原文还长）在任意 θ ≥ 0 下都被拒** —— 这正是 spike39 实测的病态区间：尾注开销 1003 字符 = 原文 41%，修复后 = 候选的 2.05×；F1 155→454（原文 461）、F4 198→409（原文 424），ROI 分别 0.02 / 0.07。**修复档原先无论值不值一律补全，是纯代价盲**。
- 实测 6 例 ROI：F1=0.02 / F2=1.48 / F3=0.80 / F4=0.07 / F5=0.29 / F6=2.14，聚合 0.49 —— θ=1 下仅 F2/F6 存活，其余退回原文保面。
- 被拒即退回 v1.1 硬拒（原文保面），错误方向仍只往「少压」错；单列 `hlsRoiSkipped` 台账（plan + `CompressRecord`）度量该区间频率。

**经济学（修正后）**：被拒 extract 的表面收益从 **0 → max(0, N)**，仅在 `N ≥ θ·C` 时兑现；代价盲区间（`N ≤ 0`）不再产生「越修越长」的负收益。spike34 语料（file:line 4/6 失败、key=value 改写 1/6）可直接度量挽回率。

**不变的部分**：summary 档维持审计放行；版本链硬排除（`rNeedCompress` 决策①）不动；`false` 显式不压不动。

---

## 4. 组件 C —— 起源类（P2，仅规格）

图目前只建模「谁引用了什么」，未建模「什么**可再派生**」。同样大小的两个原子，剪除代价不同：

- **rederivable**：read / ls / glob / grep 等对活文件的输出——剪了可以重读；
- **unique**：bash 副作用输出、web_fetch（内容会漂移）、全部 U 消息、A 结论——剪了不可再生。

**规格**：`GateOptions` 的 tool 通道从 `Map<toolName, NeedCompress>` 扩展为携带 `evictionClass`（默认启发式按工具种类名：`buildToolNameIndex` 已有索引底座）；Stage-2 剪枝序在「入度档相同、边权重相同」时，rederivable 先于 unique 摘除；与闭包生命周期组合（整 rederivable 闭包可更早退休）。

**为何 P2**：收益依赖工具分类精度与会话负载分布（文件读取密集会话受益最大）；先落 A/B 把 token 本体地基坐实，起源类是其上的第二轴。

---

## 5. 落地计划与验收

| 步 | 内容 | 出口判据 |
|---|---|---|
| Spike 38 | 推断边在真实 trace（`07-real-trace` / `26-tlong-coding` 语料）重放：0-cite 提示词冲突条件下的保护覆盖率、剪枝序 diff | I-A1..A4 全过；声明满配时 diff = ∅ |
| Spike 39 | HLS 在 spike34 语料上跑：token 挽回率、表面收益、`restoredByGuard` 台账完整性 | I-B1..B4 全过；挽回收益 > 0 且无审计丢失 |
| Spike 40 | 推断边误连率（受控植入真值）：precision/recall 曲线、半公共 handle、上限拥挤、代价侧保护面 | 度量与解析式一致；HIGH/MEDIUM 发现逐条留档（见 §2.1） |
| Spike 41 | 真实 session 语料校准（只读本机 `~/.dsh/sessions`）：读取器完整性、停词阈命中、支撑强度、影响半径 | 读取器 seq 零缺口/零重复；真实语料结论逐条留档（见 §2.2）；无 session 时优雅跳过 |
| Property tests | I-A/I-B 全部入 `test/`（随机提案扰动、随机图、随机停词分布） | `npm test` 全绿 |
| 门控单测 | `hlsRepairEconomics` 口径 + θ 灵敏度 + tool/info 两路门控落地/退回（I-B5） | `npm test` 全绿（234/234） |
| 回归 | 现有 30/60 轮 0-error、探针 7/7、前缀指纹稳定性、预算收敛（200K→160K→32K） | 逐位一致 |
| Dogfood | 落地后本插件正在当前 dsh profile 运行——`citeStats` 扩展 declared/inferred/both 三计数 + `restoredByGuard` 计数，10 轮真实会话观测 | 0 声明臂的召回效率恢复可测 |
| 文档 | CHANGELOG v1.2.0 + README「模型要求」节改写：「语义选择性不再依赖单一模型通道；保真由构造而非裁决」 | 发布 |

**配置开关（默认开、可退）**：`disableInferredEdges`、`hlsMode: 'trailer' | 'off'`、`hlsRoiThreshold`（HLS 经济学门槛 θ，缺省 1；设 0 = 只要求净释放为正，设 `Infinity` = 完全关闭修复）——任何一条不变式被证伪时可单独关停，不影响其余机制。

---

## 6. 非目标

- 不引入新运行时依赖；不用 embedding 模型；Stage-2 0-LLM 契约不变；
- recall 契约不变（`recall_summary` / `recall_detail` 逐字节）；
- 版本链硬排除、no-op 守卫、tail-only 替换、闭包生命周期语义均不变；
- 不动 CiteDeclarer 协议：声明边仍是最强证据、唯一带 level 三档语义的边源，推断边不与其竞争。

---

## 7. 与另一方向的关系（修仙模拟小模型试验）

本提案是**基座层**改动，与修仙模拟的 Spark-X2.5-4B / MiniCPM5-2B 试验（**应用层验证**）正交、可并行。

停车场备注（供后续试验直接取用）：

- **MiniCPM5-2B**：OpenBMB，2.5B 参数端侧 SOTA（2026 发布）；**`q4f16_1` MLC/WebLLM 构建已在 HuggingFace**（`ozhyhinas/MiniCPM5-2B-q4f16_1-MLC`），与现有 `Qwen3.5-2B-q4f16_1-webgpu-mlc.wasm` 形态直接可替换；
- **Spark-X2.5**（4B / 1.7B）：iFlytek 端侧 **1M 上下文**模型（2026-09 发布），长上下文对叙事连续性有利；MLC/WebLLM 可用性待确认；
- 现有评测资产可直接复用：`scripts/llm_quality_test.ts` / `llm_style_analysis.ts` + `ai-flavor-less` skill（六层统计指纹、基线 ±2σ 校准），以 Qwen3.5-2B 的既有报告为基线对照。
