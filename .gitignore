node_modules/
# 依赖 bump 时保留的旧版 node_modules 备份（本地物料，不进仓库）
node_modules.old-*/
dist/
*.log
*-out.txt
.env
sessions/
spike/out/
# npm 下载缓存（.npm-cache 是本机 npm_config_cache 落点，不进基线）
.npm-cache/
# 一次性调试/诊断脚本（probe/diag 等），不进基线
.tmp/
*.tmpdir/

# 内部开发文档：`docs/` 整个目录为**本地文档区**，一律不进公开仓库。
# 内含设计基准 / 审计报告 / 受控跑批规格与任务书 / 内部发布稿 / 对外示例（dsh-llm-adapter、
# dsh-argp-mount-example）/ token-ontology 提案等；保留在本地磁盘供开发使用。
# （原逐文件列出的 docs/*.md 排除规则已被这一条整体覆盖。）
docs/
blocker-log.md
run-baseline.bat

# AI 工作台账（本机会话记忆，不进公开仓库）
.workbuddy/

# 发布物料草稿（内部）
release-post-draft.md

# npm pack 产物（本地安装物料，不进仓库）
*.tgz

# session 分析工具（隐私铁律：session 派生数据不进仓库；脚本本身只读不落数据）
.sess-tools/
